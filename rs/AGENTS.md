# Agents Guide — proto (Rust)

The Rust port of `@tabnas/proto`, crate `tabnas-proto`, library
`tabnas_proto`. TypeScript (`../ts`) is canonical; this crate and the Go
port (`../go`) track it. Read [`../AGENTS.md`](../AGENTS.md) first: the
grammar conventions, the walk and the abnf-inlining gotcha, the output
shape, the protoc conformance bar and the untrusted-input rules are the
same in every runtime and are written down once, there.

## Layout

```
rs/Cargo.toml              package tabnas-proto, lib tabnas_proto
rs/Cargo.lock              committed
rs/src/lib.rs              plugin, options, engine/make/parse/to_descriptor, VERSION
rs/src/grammar.rs          GENERATED by ../ts/embed-grammar.js -- do not edit
rs/src/descriptor.rs       output types + the scalar table
rs/src/detect_version.rs   syntax/edition detection + reconciliation
rs/src/build_descriptor.rs the CST -> FileDescriptorProto walk
rs/src/node.rs             CST node accessors (the canonical R/kw/child)
rs/src/jsnum.rs            Number(string) as ECMA-262 defines it
rs/src/error.rs            ProtoError
rs/tests/                  parity, divergent, protobuf conformance, proto,
                           embed, version, untrusted, common/mod.rs
```

Each `src/*.rs` names the `ts/src/*.ts` file it ports in its module
comment. A change to one belongs beside a change to the other.

## Build and test

Nothing is fetched: the engine, the ABNF compiler and the shared fixture
runner are PATH dependencies on sibling checkouts. Clone
`https://github.com/tabnas/parser`, `https://github.com/tabnas/abnf`,
`https://github.com/tabnas/bnf` and `https://github.com/tabnas/support`
beside this repository. `tabnas-bnf` has no entry in `Cargo.toml` —
`tabnas-abnf` is what depends on it — but cargo reads the whole manifest
graph before it compiles anything, so the checkout has to be on disk.

```bash
cargo test --all-targets && cargo test --doc
```

`--all-targets` does NOT include doctests, and `README.md` is doctested
through `#[cfg(doctest)] #[doc = include_str!("../README.md")]`, so both
selections matter. `make test-rs` from the repository root runs both plus
clippy; `ci/rust/run.sh` is the full gate, including `cargo fmt --check`
and the lockfile comparison, and it restores `Cargo.lock` on the way out
whether it passed or failed.

## What "correct" means here

Same order of authority as the repository guide, with one addition.

1. **The shared fixtures pass**, every row of every `../test/spec/*.tsv`,
   run by `tests/parity_test.rs` through `tabnas_support::Runner` — the
   same loader `ts/test/parity.test.ts` and `go/parity_test.go` use, so
   the three cannot drift on what a row means.
2. **The protoc corpus passes**, `tests/protobuf_conformance_test.rs`,
   with the same lanes, contracts and normalisation the other two
   runners apply: `valid` (71 in-scope cases, compared against protoc's
   goldens), `accept-only` (50, parse without failing) and the
   `leniency` probes. Two things are NOT run, in every runtime alike and
   for the reasons `../AGENTS.md` gives: the 11 `valid` cases declaring
   a protoc-internal edition, with the exclusion set asserted to be
   exactly those, and the whole `invalid` lane, because `common.abnf` is
   a permissive union and rejection is not part of the contract. Both
   counts are ratcheted in the test, so a corpus that shrinks cannot
   pass by measuring less.
3. **The generated grammar matches its source.** `src/grammar.rs` is
   written by `../ts/embed-grammar.js`, guarded on `rs/src` existing.
   After editing any `.abnf` file run `npm run embed` from `ts/` AND
   `make generate` for Go. `tests/embed_test.rs` compares the Rust copy
   with the files on disk and with both other runtimes' embeds.
4. **The five version sites agree**: `ts/package.json`, `VERSION` in
   `ts/src/proto.ts`, `const VERSION` in `go/proto.go`, `version` in
   `Cargo.toml` here and `pub const VERSION` in `src/lib.rs`.
   `tests/version_test.rs` fails the build on drift, and the release
   procedure in `../AGENTS.md` names the same five with the check that
   catches each. Bumping `Cargo.toml` moves `Cargo.lock`'s entry for
   this crate too, which `../ci/rust/run.sh` compares before it runs
   anything.
5. **Every divergence is recorded AND executed.**
   `../DIVERGENCE.md` carries the prose and the measured tables;
   `../test/divergent.tsv` carries the rows, and
   `tests/divergent_test.rs` runs them through
   `tabnas_support::Register`. A row that stops being true fails, and so
   does a row whose divergence has been REPAIRED: the register names the
   row to delete rather than letting a stale claim sit there green.

## Things this port had to decide

The canonical walk is JavaScript, and three of its behaviours come from
the language rather than from `.proto`. Each is reproduced deliberately,
with the reason written at the point it appears:

- **`Number(string)` is not `str::parse`.** A NonDecimalIntegerLiteral
  takes no sign, so `-0x10` is `NaN` and the walk keeps the text; a
  numeric separator is not part of StringToNumber, so `1_0` is `NaN` even
  though the shared lexer tokenises it; and JavaScript's whitespace is
  `\p{White_Space}` minus U+0085 plus U+FEFF. `src/jsnum.rs` is that
  reading, and nothing here goes the other way: no number is turned back
  into text.
- **A prototype-reaching key.** `map[name] = value` on a bare object
  DROPS an option named `__proto__`, because the assignment sets the
  prototype instead of creating a property. `read_field_options` drops it
  for the same reason, on purpose. An `option __proto__ = ...;` statement
  is a different path, a computed-key object literal, which DOES create
  an own property, so that one survives in every runtime.
  `SCALAR_TYPES[name]` reaches the prototype too, and that one cannot be
  reproduced: see `../DIVERGENCE.md`.
- **Character classes are ASCII.** Every canonical pattern here is a
  JavaScript regexp whose `\d`, `\s` and `\w` are ASCII, and the `regex`
  crate's are Unicode-aware. None of them is handed to a regexp engine;
  each is written out, with the pattern it replaces quoted above it.

## Reading `src` is allowed; DECIDING from it is not

`src` is the node's tokens run together, and the walk reads values out of
it wherever abnf inlined the rule that would have carried them: the
leading field type, the first `reserved` range, an option name inside
`[...]`. That is reading a value the tree does not hold, and it is safe,
because whole-word tokens make the boundaries unambiguous.

What is NOT safe is answering a STRUCTURAL question by searching that
text, and issue #31 is the class. A search finds a copy, not the copy:
`(stream` matched the start of `streaming.Request`, `=-` inside an
option took an enum value negative, `kw` found the `m` of `message`
rather than the message named `m`. Each had a node or a gap that held
the answer already.

Use `nrule`, `child`, `children`, `child_rules`, `kw` and `gaps`. `gaps`
is the one to reach for when the answer is a bare terminal the grammar
never turns into a node: it gives the text between two adjacent rule
children, scanning from the end so each child is bounded by the child
after it.

What still reads `src`, and why each one stays:

- `ranges` and `reserved_names` parse a statement's text, because the
  leading `range` and a single-item name list are inlined and there is
  no node at all. Both consume the whole text rather than searching it.
- `option_name_of` falls back to the text when `optionName` was inlined,
  anchored at the END on `=<constant>`; anchoring at the start would
  find the `1` of `file_opt1`.
- `detect_version` reads the `syntax` / `edition` declaration out of the
  source before any walk, which is the point of it.
- The dispatch tests (`kw(node).starts_with("message")` and the rest)
  read the KEYWORD, which is a gap and not a search. They were unsound
  only while `kw` itself searched forward.

`build_descriptor.rs` has no other `contains`, `find` or index over
flattened source deciding a structural question, as of 2026-09-22.

## Untrusted input, and the one bound this port adds

Everything in the repository guide applies. The addition is a NESTING
CAP, `MAX_NESTING_DEPTH`. The descriptor walk recurses, `tabnas::Value`
drops recursively and `Value::to_json` recurses; a Rust stack that runs
out ABORTS the process, where a JavaScript one raises something a caller
can catch. `parse` counts braces outside strings and comments and refuses
a document past the cap before the engine builds a tree that deep, and
`build_file` refuses one it is handed directly.

Refusing a CST is the weaker half, and it is not the protection: the
tree already exists by then, and a `Value` nesting far enough aborts as
it DROPS. What protects a caller is never building one, so every entry
point taking SOURCE runs the check. `parse_with(&parser, src, opts)` is
the reusable form, for the caller holding an instance from `make`, and
`preflight(src)` is the check alone, for the caller who wants the CST
from the engine's own `parse`. The README recommended that raw route
without either, which is issue #30: a fast path around the crate's own
guard, reachable from untrusted input.

The number is MEASURED, and the measurement is in the constant's doc
comment. Re-measure rather than copying it if the walk changes shape, and
keep `tests/untrusted_test.rs` parsing AT the cap: a cap nobody tests at
is a number, not a bound.

## A rule with many elements is quadratic, and the engine owns it

Parsing scales linearly in the LENGTH of a token and quadratically in the
number of elements inside ONE repetition, which for `.proto` means the
fields in one message, the names in one `reserved` list, and the
definitions in one file. Measured here on the debug profile, parsing a
single message of N fields:

| fields | Rust (debug) | canonical TypeScript |
|---|---|---|
| 50 | 0.35s | 0.056s |
| 100 | 0.84s | 0.043s |
| 200 | 2.75s | 0.093s |
| 400 | 10.1s | 0.129s |
| 800 | 46.6s | — |

Splitting the three passes puts it beyond doubt: at 400 fields the engine
parse takes 13.26s where the descriptor walk takes 0.030s and the drop
0.004s. The walk is linear; the tree build is not.

**The cause is not in this crate, and not in the grammar.** It is the
same defect `../../abnf/rs/AGENTS.md` diagnoses: `Rule::accept_child_node`
in `../../parser/rs/src/rule.rs` does
`self.child_node = child.node.borrow().clone()`, and a `Value` container
sits behind an `Arc`, so that clone is a second reference to the very
array the next `push_node` mutates. `Arc::make_mut` then copies the whole
array once per element. The engine already computes `child_node_is_self`
on the line above, which is the condition under which the clone is the
parent's own accumulator and can be skipped.

Every answer is the same in all three runtimes; only the cost differs, so
this is NOT in `../DIVERGENCE.md`. It is also not something to bound away
here: a message with 800 fields is an ordinary `.proto` file, and a cap
on it would reject legitimate input to hide an engine defect.

`tests/untrusted_test.rs` measures the token-length dimension, which is
linear, and asserts an absolute ceiling rather than a ratio. **Do not add
a ratio assertion for the element-count dimension**: it would pin the
quadratic as acceptable and go green forever. The number is recorded
here instead, and belongs to whoever fixes the engine.

## Documentation

`README.md` is in the gated set (`ts/scripts/gated-docs.cjs`), so the
prose rules in `../docs/STYLE-GUIDE.md` apply to it: no em dash, no first
person, and the banned-phrase list. Every `rust` fence in it is a
complete `fn main` program compiled as a doctest. Rust terms of art the
Vale dictionary lacks belong in
`../.vale/styles/config/vocabularies/Tabnas/accept.txt`, and adding a
page moves the counts recorded in `../.vale.ini` and
`../docs/STYLE-GUIDE.md`: re-measure with `make prose-counts`.

This guide is NOT in the gated set, and uses em dashes by house
convention, as the other agent guides do.
